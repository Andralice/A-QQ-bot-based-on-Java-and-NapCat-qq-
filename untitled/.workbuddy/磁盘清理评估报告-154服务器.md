# 154.8.213.134 磁盘清理评估报告

> 生成时间：2026-08-05 14:45
> 当前状态：`/dev/vda2` 40G，已用 37G，**可用 642M（99%）**
> 本报告仅评估，**未执行任何清理**

---

## 一、服务器上正在跑什么（清理前必须知道）

| 服务 | 形态 | 状态 | 关键依赖路径 |
|---|---|---|---|
| 糖果熊 QQ Bot | screen + java | ✅ 运行中（今日 14:37 重启） | `/opt/qq-bot/*.jar`、`screenshot.py` |
| NapCat / QQ | 原生进程 | ✅ 运行中 | `/root/Napcat`、`/home/alice/.config/QQ` |
| test-platform | docker × 2 | ✅ Up 2 days | 镜像 1.2G |
| agent-platform | docker × 3 | ✅ Up 7 days / mysql 5 weeks | 镜像 1.9G |
| Andlri | docker × 4 | ⚠️ **backend 已挂 2 周** | 见下方警告 |
| 未知 Java 服务 | `java -jar app.jar` | ✅ 运行中 | PID 1168456 |
| Python 服务 | `:3000` | ✅ 运行中 | PID 670943 |
| Node 服务 | `:8088` | ✅ 运行中 | PID 431353 |

### ⚠️ 顺带发现的问题（与清理无关，但建议处理）

`andlri-backend` 容器 **Exited (137)，已停 2 周**。137 = SIGKILL，通常是 OOM 或磁盘写满被内核杀掉。
nginx / postgres / redis 三个容器还活着，但后端已死，所以 Andlri 实际上处于**半瘫痪状态**（`curl 127.0.0.1` 返回 000）。

---

## 二、A 级：零风险，建议直接清 —— 约 **10.7 GB**

这些都是一次性打包、构建残留或缓存，删掉不影响任何运行中的服务。

| # | 路径 | 大小 | 时间 | 判断依据 |
|---|---|---|---|---|
| 1 | `/tmp/candybear.tar.gz` | **3.6G** | 07-27 | 一次性源码归档，无进程占用 |
| 2 | `/tmp/candybear_git.tar.gz` | **2.6G** | 07-22 | 同上，含 .git 历史的旧打包 |
| 3 | `/tmp/qq-bot.tar.gz` | 929M | 07-22 | 旧项目打包 |
| 4 | `/tmp/qq-bot-src.tar.gz` | 828M | 07-22 | 旧源码打包 |
| 5 | 悬空 docker 镜像 × 2 | **1.38G** | 7天前/3周前 | `dangling=true`，无容器引用 |
| 6 | journal 日志收缩至 200M | ~610M | — | `journalctl --vacuum-size=200M` |
| 7 | `/tmp/mobile-build/` | 235M | 07-10 | 前端构建目录（含 node_modules） |
| 8 | `/tmp/154-app-*.deleted-20260722-*` × 3 | 149M | 07-20~22 | 文件名已自带 `.deleted` 标记 |
| 9 | `/var/cache/apt` | 138M | — | apt 下载缓存，`apt clean` 即可 |
| 10 | `/tmp/*.bundle` + `candybear-src.tar.gz` × 3 | 120M | 07-22~24 | git bundle 传输中间物 |
| 11 | `/tmp/app-new.jar` + `app-check.jar` | 92M | 07-10 | 一个月前的临时 jar |
| 12 | `/tmp/agent-platform-deploy2.tar.gz` | 56M | 07-17 | 部署包，已部署完成 |

**A 级小计：约 10.7 GB** —— 清完可用空间从 642M 涨到约 **11.3 G**。

---

## 三、B 级：低风险，但要你点头 —— 约 **5.8 GB**

| # | 路径 | 大小 | 影响说明 |
|---|---|---|---|
| 1 | docker build cache | **3.5G** | 可回收部分。清后下次 `docker build` 变慢（重新拉层），**不影响已运行容器** |
| 2 | `/root/.npm/_cacache` | 494M | npm 下载缓存。清后下次 `npm i` 走网络 |
| 3 | `/opt/qq-bot/qq-bot.log` | **445M** | ⚠️ 正在写入，必须用 `: > file` 截断，**不能 rm**（rm 会导致 inode 泄漏，空间不释放） |
| 4 | `/opt/qq-bot/_deploy/` | 257M | 07-22 的部署副本，与当前 `/opt/qq-bot/untitled` 重复 |
| 5 | `/opt/andlri/andlri-frontend-tmp` | 247M | 命名即 tmp，需确认不是当前 nginx 挂载源 |
| 6 | `/opt/qq-bot/target.old/` | 196M | 06-26 的旧构建产物 |
| 7 | `/opt/qq-bot/untitled/target/` | 196M | 今天刚构建的，jar 已复制走，可删 |
| 8 | 旧 jar 备份 × 2 | 194M | 已按策略保留最近 3 个，可再减到 1 个 |
| 9 | `/opt/andlri/andlri-source.bak-20260716.tar.gz` | 134M | 三周前的源码备份 |
| 10 | `/opt/qq-bot/src.old/` | 60M | 2025-12-30 的源码，超过半年 |
| 11 | `/var/log/*.1` 与旧 `.gz` | ~50M | logrotate 归档 |
| 12 | `/opt/qq-bot/novel-agent.log` | 21M | 06-26 停更 |
| 13 | `/opt/qq-bot/qq-bot.stdout.log` | 14M | 07-20 停更 |
| 14 | `/opt/qq-bot/bot.log` | 2.7M | 01-01 停更 |

**B 级小计：约 5.8 GB**

---

## 四、C 级：**绝对不要动**

| 路径 | 大小 | 原因 |
|---|---|---|
| `/home/alice/.cache/ms-playwright` | **613M** | 🔴 **截图功能的命脉**，Chromium 浏览器本体就在这。删了三角洲截图立刻全挂 |
| `/home/alice/.config/QQ` | 657M | 🔴 QQ 登录态，删了要重新扫码登录 |
| `/root/Napcat/opt` | 647M | 🔴 NapCat 本体，运行中 |
| `/root/.m2/repository` | 167M | 服务器 Maven 本地仓库。今天的构建就是靠它离线完成的，删了下次构建要重新下载全部依赖 |
| `/tmp/+~JF*.tmp` × 7 | 57M | 当前 java 进程（PID 3812294）持有的 JVM 临时文件 |
| `andlri-backend:latest` 镜像 | 839M | 容器虽然挂了，但镜像还需要用来恢复服务 |
| 所有运行中容器的镜像 | ~4.5G | 略 |
| `/opt/qq-bot/untitled/src/main/resources/assets` | 58M | 打包必需资源 |

---

## 五、建议的执行顺序

1. **先做 A 级**（10.7G，零风险）→ 可用空间回到 11G 左右，服务器立刻脱离危险区
2. **给 journald 加个上限**，防止再涨回去：
   `/etc/systemd/journald.conf` 加 `SystemMaxUse=200M`
3. **给 qq-bot.log 配 logrotate**，当前日志是 DEBUG 级别且打印 OneBot 全量原始事件，7 天就能涨到 445M。要么调日志级别，要么加轮转
4. **B 级按需选**，其中 docker build cache 3.5G 是最大头，代价只是下次构建慢一点
5. **单独处理 andlri-backend**，它挂了 2 周，很可能就是这次磁盘写满导致的连锁反应

---

## 六、需要你确认的三件事

1. `/opt/andlri/andlri-frontend-tmp`（247M）和 `andlri-frontend-src`（249M）—— 哪个是 nginx 实际挂载的？我不敢乱动
2. `platform-v2` 容器已 Exited 2 天，`platform` 镜像 6 周前 —— 这两套是不是已被 `test-platform` 取代？如果是，镜像可再释放约 1.2G
3. `java -jar app.jar`（PID 1168456）是哪个项目？它的 jar 在哪个目录，我好判断相关备份能不能清
